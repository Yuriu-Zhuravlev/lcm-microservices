apiVersion: v1
kind: ConfigMap
metadata:
  name: auth-service-config
  namespace: {{ include "lms.namespace" . }}
  labels:
    {{- include "lms.labels" . | nindent 4 }}
data:
  SPRING_DATASOURCE_URL: {{ .Values.authService.datasourceUrl | quote }}
  SPRING_DATA_REDIS_HOST: {{ .Values.global.redis.host | quote }}
  SPRING_DATA_REDIS_PORT: {{ .Values.global.redis.port | quote }}
  EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: {{ .Values.eurekaUrl | quote }}
{{- if not .Values.eso.enabled }}
---
apiVersion: v1
kind: Secret
metadata:
  name: lms-secrets
  namespace: {{ include "lms.namespace" . }}
type: Opaque
stringData:
  SPRING_DATASOURCE_USERNAME: {{ .Values.postgres.user | quote }}
  SPRING_DATASOURCE_PASSWORD: {{ .Values.postgres.password | quote }}
  JWT_SECRET: {{ .Values.jwtSecret | quote }}
{{- end }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: {{ include "lms.namespace" . }}
spec:
  {{- if not .Values.authService.hpa.enabled }}
  replicas: {{ .Values.authService.replicas }}
  {{- end }}
  selector:
    matchLabels:
      app: auth-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: auth-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/actuator/prometheus"
        prometheus.io/port: "8081"
    spec:
      initContainers:
        - name: wait-for-deps
          image: busybox:1.36
          command: ['sh', '-c', 'until nc -z {{ .Values.global.postgres.host }} 5432 && nc -z {{ .Values.global.redis.host }} 6379 && nc -z eureka-server 8761; do sleep 3; done']
      containers:
        - name: auth-service
          image: {{ .Values.global.imageRegistry }}/{{ .Values.authService.image }}:{{ .Values.global.imageTag }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.authService.port }}
          envFrom:
            - configMapRef:
                name: auth-service-config
            - secretRef:
                name: lms-secrets
          resources:
            {{- toYaml .Values.authService.resources | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.authService.port }}
            initialDelaySeconds: 40
            periodSeconds: 10
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.authService.port }}
            initialDelaySeconds: 120
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: {{ include "lms.namespace" . }}
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
    - port: {{ .Values.authService.port }}
      targetPort: {{ .Values.authService.port }}