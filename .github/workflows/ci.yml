name: CI/CD Pipeline

on:
  push:
    branches: [ "master" ]

env:
  REGISTRY: ghcr.io
  IMAGE_TAG: latest

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Run fast build
        run: mvn clean compile -DskipTests

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push Eureka Server
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./eureka-server/Dockerfile
          push: true
          tags: ${{ env.REGISTRY }}/${{ github.repository }}/eureka-server:${{ env.IMAGE_TAG }}

      - name: Build and Push Api Gateway
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./api-gateway/Dockerfile
          push: true
          tags: ${{ env.REGISTRY }}/${{ github.repository }}/api-gateway:${{ env.IMAGE_TAG }}

      - name: Build and Push Auth Service
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./auth-service/Dockerfile
          push: true
          tags: ${{ env.REGISTRY }}/${{ github.repository }}/auth-service:${{ env.IMAGE_TAG }}

      - name: Build and Push Course Service
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./course-service/Dockerfile
          push: true
          tags: ${{ env.REGISTRY }}/${{ github.repository }}/course-service:${{ env.IMAGE_TAG }}

      - name: Build and Push Learning Service
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./learning-service/Dockerfile
          push: true
          tags: ${{ env.REGISTRY }}/${{ github.repository }}/learning-service:${{ env.IMAGE_TAG }}